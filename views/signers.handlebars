<h3>Our Supporters 🎗</h3>
<ul class="petionerslist">
    {{#results}}
        <li>
            {{#if url}}
            <a href=`{{url}}`>{{fname}}{{lname}}</a> {{age}} <a href="/signers/:{{city}}">{{city}} </a>
                        {{#else}}
{{fname}} {{lname}} {{age}} <a href="/signers/:{{city}}">{{city}} </a>
            {{/if}}
            </li>
    {{/results}}
</ul>